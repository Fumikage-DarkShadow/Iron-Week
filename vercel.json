{
  "buildCommand": "npx expo export --platform web --output-dir dist && node scripts/post-build.js",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
